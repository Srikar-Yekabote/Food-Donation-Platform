<!DOCTYPE html>
<html lang="en">
<head>
    <title>Edit Food</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body >
    <%- include("partials/navbar") %>
    <div class="bg-gray-100 min-h-screen flex flex-col items-center justify-center">
    <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-xl mt-6">
        <h1 class="text-2xl font-bold mb-6 text-center text-blue-600">Edit Your Food Donation 🍱</h1>

        <form action="/edit-food/<%= food._id %>" method="POST" class="space-y-4">
            <div>
                <label class="block font-semibold">Food Name</label>
                <input type="text" name="name" value="<%= food.name %>" required class="w-full p-2 border rounded" />
            </div>

            <div>
                <label class="block font-semibold">Quantity</label>
                <input type="number" name="quantity" value="<%= food.quantity %>" required class="w-full p-2 border rounded" />
            </div>

            <div>
                <label class="block font-semibold">Expiry Date</label>
                <input type="date" name="expiryDate" value="<%= food.expiryDate.toISOString().substring(0, 10) %>" required class="w-full p-2 border rounded" />
            </div>

            <div>
                <label class="block font-semibold">Address</label>
                <input type="text" name="address" value="<%= food.address %>" required class="w-full p-2 border rounded" />
            </div>

            <button type="submit" class="bg-blue-500 text-white px-6 py-2 rounded hover:bg-blue-600">Update</button>
        </form>
    </div>
</div>
</body>
</html>
